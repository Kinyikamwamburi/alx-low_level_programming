#include "main.h"
#include <string.h>

/**
 * _strcmp - Write a function that compares two strings.
 *
 * @s1: compared to the other string
 * @s2: compared to the other string
 * value - returns compared string
 *
 * Return: return compared strings
 */

int _strcmp(char *s1, char *s2)
{
	int value = strcmp(s1, s2);

	return (value);
}

